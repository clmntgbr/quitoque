{% extends 'base.html.twig' %}

{% block title %}
{% endblock %}

{% block stylesheets %}
{% endblock %}

{% block body %}
    <table>
        <thead>
        <tr>
            <th>Titre</th>
            <th>Auteur</th>
            <th>Année de publication</th>
            <th>Genre</th>
        </tr>
        </thead>
        <tbody>
            <tr>
                <td><a href="{{ path('app_book_id', {'id': book.id}) }}">{{ book.title }}</a></td>
                <td><a href="{{ path('app_book_author', {'id': book.bookAuthor.id}) }}">{{ book.bookAuthor.name }}</a></td>
                <td><a href="{{ path('app_book_release', {'date': book.releaseDate}) }}">{{ book.releaseDate }}</a></td>
                <td><a href="{{ path('app_book_type', {'id': book.bookType.id}) }}">{{ book.bookType.name }}</a></td>
            </tr>
        </tbody>
    </table>

{% endblock %}

{% block javascripts %}
{% endblock %}
